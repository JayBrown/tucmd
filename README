tucmd.py - Python 3 program & library to upload a file to tinyupload.com


Installation on macOS:

* install either Xcode or the Command Line Tools (this will also install python3, currently at v3.8)
* clone this repo
* symlink to tulib.py in the cloned repo from ~/Library/Python/3.8/lib/python/site-packages/tulib.py
* symlink to tucmd.py in the cloned repo from one of your $PATH's bin directories, e.g. from /usr/local/bin/tulib.py or from ~/.local/bin/tulib.py


usage: tucmd.py [-h] [-v] [-f FILE] upload

A program to upload files to tinyupload.com

positional arguments:
  upload                Upload local file

optional arguments:
  -h, --help            show this help message and exit
  -v, --version         show program's version number and exit
  -f FILE, --file FILE  Local filepath


This version drops the download and file segmentation functionalities; if you need any of those, please use the original v1.0: https://github.com/marcomg/tinyuploadmanager